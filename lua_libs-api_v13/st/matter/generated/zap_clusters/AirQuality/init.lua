-- Copyright 2022 SmartThings
--
-- Licensed under the Apache License, Version 2.0 (the "License");
-- you may not use this file except in compliance with the License.
-- You may obtain a copy of the License at
--
-- http://www.apache.org/licenses/LICENSE-2.0
--
-- Unless required by applicable law or agreed to in writing, software
-- distributed under the License is distributed on an "AS IS" BASIS,
-- WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
-- See the License for the specific language governing permissions and
-- limitations under the License.

-- DO NOT EDIT: this code is automatically generated by ZCL Advanced Platform generator.

local cluster_base = require "st.matter.cluster_base"
local AirQualityServerAttributes = require "st.matter.generated.zap_clusters.AirQuality.server.attributes"
local AirQualityServerCommands = require "st.matter.generated.zap_clusters.AirQuality.server.commands"
local AirQualityTypes = require "st.matter.generated.zap_clusters.AirQuality.types"

--- @class st.matter.generated.zap_clusters.AirQuality
--- @alias AirQuality
---
--- @field public ID number 0x005B the ID of this cluster
--- @field public NAME string "AirQuality" the name of this cluster
--- @field public attributes st.matter.generated.zap_clusters.AirQualityServerAttributes | st.matter.generated.zap_clusters.AirQualityClientAttributes
--- @field public commands st.matter.generated.zap_clusters.AirQualityServerCommands | st.matter.generated.zap_clusters.AirQualityClientCommands
--- @field public types st.matter.generated.zap_clusters.AirQualityTypes

local AirQuality = {}

AirQuality.ID = 0x005B
AirQuality.NAME = "AirQuality"
AirQuality.server = {}
AirQuality.client = {}
AirQuality.server.attributes = AirQualityServerAttributes:set_parent_cluster(AirQuality)
AirQuality.server.commands = AirQualityServerCommands:set_parent_cluster(AirQuality)
AirQuality.types = AirQualityTypes

--- Find an attribute by id
---
--- @param attr_id number
function AirQuality:get_attribute_by_id(attr_id)
  local attr_id_map = {
    [0x0000] = "AirQuality",
    [0xFFF9] = "AcceptedCommandList",
    [0xFFFA] = "EventList",
    [0xFFFB] = "AttributeList",
  }
  local attr_name = attr_id_map[attr_id]
  if attr_name ~= nil then
    return self.attributes[attr_name]
  end
  return nil
end

--- Find a server command by id
---
--- @param command_id number
function AirQuality:get_server_command_by_id(command_id)
  local server_id_map = {
  }
  if server_id_map[command_id] ~= nil then
    return self.server.commands[server_id_map[command_id]]
  end
  return nil
end


-- Attribute Mapping
AirQuality.attribute_direction_map = {
  ["AirQuality"] = "server",
  ["AcceptedCommandList"] = "server",
  ["EventList"] = "server",
  ["AttributeList"] = "server",
}

do
  local has_aliases, aliases = pcall(require, "st.matter.clusters.aliases.AirQuality.server.attributes")
  if has_aliases then
    for alias, _ in pairs(aliases) do
      AirQuality.attribute_direction_map[alias] = "server"
    end
  end
end

-- Command Mapping
AirQuality.command_direction_map = {
}

do
  local has_aliases, aliases = pcall(require, "st.matter.clusters.aliases.AirQuality.server.commands")
  if has_aliases then
    for alias, _ in pairs(aliases) do
      AirQuality.command_direction_map[alias] = "server"
    end
  end
end

do
  local has_aliases, aliases = pcall(require, "st.matter.clusters.aliases.AirQuality.client.commands")
  if has_aliases then
    for alias, _ in pairs(aliases) do
      AirQuality.command_direction_map[alias] = "client"
    end
  end
end

AirQuality.FeatureMap = AirQuality.types.Feature

function AirQuality.are_features_supported(feature, feature_map)
  if (AirQuality.FeatureMap.bits_are_valid(feature)) then
    return (feature & feature_map) == feature
  end
  return false
end

-- Cluster Completion
local attribute_helper_mt = {}
attribute_helper_mt.__index = function(self, key)
  local direction = AirQuality.attribute_direction_map[key]
  if direction == nil then
    error(string.format("Referenced unknown attribute %s on cluster %s", key, AirQuality.NAME))
  end
  return AirQuality[direction].attributes[key]
end
AirQuality.attributes = {}
setmetatable(AirQuality.attributes, attribute_helper_mt)

local command_helper_mt = {}
command_helper_mt.__index = function(self, key)
  local direction = AirQuality.command_direction_map[key]
  if direction == nil then
    error(string.format("Referenced unknown command %s on cluster %s", key, AirQuality.NAME))
  end
  return AirQuality[direction].commands[key]
end
AirQuality.commands = {}
setmetatable(AirQuality.commands, command_helper_mt)

local event_helper_mt = {}
event_helper_mt.__index = function(self, key)
  return AirQuality.server.events[key]
end
AirQuality.events = {}
setmetatable(AirQuality.events, event_helper_mt)

setmetatable(AirQuality, {__index = cluster_base})

return AirQuality

